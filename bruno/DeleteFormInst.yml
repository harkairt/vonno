info:
  name: DeleteFormInst
  type: http
  seq: 14

http:
  method: POST
  url: "{{url}}/api/Form/DeleteFormInst"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "sessionId": "{{sessionId}}",
        "agentId": 1,
        "instanceId": "{{instanceId}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
