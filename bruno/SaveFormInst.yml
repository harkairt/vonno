info:
  name: SaveFormInst
  type: http
  seq: 13

http:
  method: POST
  url: "{{url}}/api/Form/SaveFormInst"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "sessionId": "{{sessionId}}",
        "agentId": 1,
        "instanceId": "{{instanceId}}",
        "data": {},
        "lastEditedField": "/name"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
